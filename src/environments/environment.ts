// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase:{
    apiKey: "AIzaSyAuss6w63L6y2lMPt2-ybKNcVBpTVh-XL8",
  authDomain: "chokkanahalli-project.firebaseapp.com",
  databaseURL: "https://chokkanahalli-project.firebaseio.com",
  projectId: "chokkanahalli-project",
  storageBucket: "chokkanahalli-project.appspot.com",
  messagingSenderId: "522007547592",
  appId: "1:522007547592:web:9bdaf9c07a09f9072a5ffd"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
